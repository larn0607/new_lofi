import React from 'react'
import Box from '../Box.tsx'

const Template = ({title}) => {
  return (
    <Box title={title} className='mt-4 pt-4'>
      template
    </Box>
  )
}

export default Template